{
  "name": "lyric-stage-workspace",
  "version": "0.0.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm --filter './packages/*' --if-present build && pnpm --filter '@lyric-stage/extension' build",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "playwright": "^1.61.1",
    "typescript": "catalog:",
    "vite": "catalog:"
  },
  "author": "oftx"
}
